<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.joy.cancerhub">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignIn" />
        <activity android:name=".activities.SignUp" />
        <activity android:name=".activities.Basic_Info" />
        <activity android:name=".activities.NewsMain" />
        <activity android:name=".activities.FindDoctor" />
        <activity
            android:name=".activities.MyHealth"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ResetPassword" />
        <activity android:name=".activities.Docs_profile" />
        <activity android:name=".activities.cancer_types" />
        <activity android:name=".activities.LungCancer" />
        <activity
            android:name=".activities.Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.Information" />
        <activity android:name=".models.LiverC" />
        <activity android:name=".activities.HealthRecords" />
    </application>

</manifest>